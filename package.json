{
  "name": "bjelectronics-hostinger-deploy",
  "private": true,
  "scripts": {
    "build": "npm run build:store",
    "start": "npm run start:store",
    "build:store": "npm install --prefix store && npm run build --prefix store",
    "start:store": "npm run start --prefix store",
    "build:admin": "npm install --prefix store && npm run build --prefix store",
    "start:admin": "npm run start --prefix store",
    "build:api": "npm install --prefix store && npm run build --prefix store",
    "start:api": "npm run start --prefix store"
  },
  "overrides": {
    "postcss": "8.5.10"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {}
}
